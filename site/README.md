# SLNT Landing Page

Static landing page for sRFC-0042 / SLNT.

Open `index.html` directly, or serve it locally:

```bash
python3 -m http.server 4173 --bind 127.0.0.1 --directory site
```

Then visit <http://127.0.0.1:4173/>.
